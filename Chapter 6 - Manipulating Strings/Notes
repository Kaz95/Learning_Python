- String Notes
    - String Methods
        - .upper()

        - .lower()

        - .isupper()

        - .islower()

        - .isalpha()
            - Only letters (and not blank)

        - .isalnum()
            - Only letters OR numbers

        - .isdecimal
            - Only numbers

        - .isspace
            - Spaces tabs and new lines(Still not blank!!!)

        - .istitle
            - Capital letter followed by lowercase.

        - .startswith('passed string')
            - Returns True if string value called on begins with string passed to method.
            - Ex: http, C:\, ect

        - .endswith('passed string')
            - Returns true if string value called on ends with string passed to method
            - Ex: .com, end of a give file directory, ect

        - 'Some string'.join([Some_list])
            - Joins a list of strings using the string value called on the method.

        - 'Some string'.split()
            - Splits string called method. Returns a list of string values.
            - If no string value is passed to method
                - Default splits on white space.
            - Common use is splitting text by \n(newline)
                - # TODO Try to manipulate Hemo log text output at some point. Edit feature.

        - Filling with white space or character
            - 'Some string'.rjust(int, )
            - 'Some string'.ljust(int, )
            - 'Some string'.center(int, )

        - Deleting white space(space, tab, and newline). Can be passed string to strip instead of whitespace.
            - 'Some string'.strip()
            - 'Some string'.lstrip()
            - 'Some string'.rstrip()
    - "" allows use of apostrophe  inside string value
    - Use \ if you need single('') and double quotes(""
    - Escape Characters
        - \t
            - Tab
        - \n
            - Start new line
        - \\
            - Backslash
    - print(r'string')
        - raw string
    - '' x3 for multi-line strings.
    - Index strings same as lists
    - In and not in work with strings